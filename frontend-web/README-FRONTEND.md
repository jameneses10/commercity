# Frontend web CommerCity

Fase 6.15 inicia reconstrucción limpia desde cero.

Stack: HTML, Tailwind CDN y JavaScript puro.

No requiere build. Servir con:

```bash
python3 -m http.server 8080 --bind 0.0.0.0
```

API esperada: `http://localhost:3000/api/v1`.
