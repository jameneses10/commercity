import {money} from '../js/ui/format.js';
export function productCard(p,base=''){
 const final=p.precio_final??p.precio_con_descuento??p.precio; const hasDiscount=Number(p.descuento_porcentaje||0)>0;
 return `<article class="cc-card overflow-hidden flex flex-col"><div class="h-44 bg-slate-100 relative"><img class="w-full h-full object-cover" src="${p.imagen_url||base+'assets/logo/Logo - Commercity.png'}" alt="${p.nombre}">${hasDiscount?`<span class="absolute top-2 left-2 bg-orange-500 text-white px-2 py-1 rounded-full text-xs font-bold">-${p.descuento_porcentaje}%</span>`:''}${Number(p.stock)===0?`<span class="absolute top-2 right-2 bg-slate-800 text-white px-2 py-1 rounded-full text-xs">Agotado</span>`:''}</div><div class="p-4 flex-1 flex flex-col"><p class="text-xs text-slate-500">${p.categoria_nombre||''} · ${p.tienda_nombre||''}</p><h3 class="font-bold text-lg line-clamp-2">${p.nombre}</h3><div class="mt-2">${hasDiscount?`<p class="text-sm text-slate-400 line-through">${money(p.precio_anterior||p.precio)}</p>`:''}<p class="text-blue-700 font-extrabold">${money(final)}</p></div><p class="text-sm text-slate-500 mt-1">Stock: ${p.stock ?? '-'} · ⭐ ${p.calificacion_promedio||0}</p><div class="mt-auto pt-4 flex gap-2"><a class="btn-secondary text-center flex-1" href="${base}pages/product-detail.html?id=${p.id}">Ver detalle</a><button class="btn-action add-cart" data-id="${p.id}" ${Number(p.stock)===0?'disabled':''}>Agregar</button></div></div></article>`
}
