import {money} from '../js/ui/format.js';
export function productCard(p,base=''){return `<article class="cc-card overflow-hidden flex flex-col"><div class="h-44 bg-slate-100"><img class="w-full h-full object-cover" src="${p.imagen_url||base+'assets/logo/Logo - Commercity.png'}" alt="${p.nombre}"></div><div class="p-4 flex-1 flex flex-col"><p class="text-xs text-slate-500">${p.categoria_nombre||''} · ${p.tienda_nombre||''}</p><h3 class="font-bold text-lg line-clamp-2">${p.nombre}</h3><p class="text-blue-700 font-extrabold mt-2">${money(p.precio)}</p><p class="text-sm text-slate-500 mt-1">Stock: ${p.stock ?? '-'} · ⭐ ${p.calificacion_promedio||0}</p><div class="mt-auto pt-4 flex gap-2"><a class="btn-secondary text-center flex-1" href="${base}pages/product-detail.html?id=${p.id}">Ver detalle</a><button class="btn-action add-cart" data-id="${p.id}">Agregar</button></div></div></article>`}
