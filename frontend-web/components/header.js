import {getUser,logout} from '../js/auth/session.js';
import {itemCount} from '../js/cart/cart.storage.js';
export function renderHeader(active=''){ const inPages=location.pathname.includes('/pages/'); const base=inPages?'../':''; const u=getUser(); const dash=u?(u.rol==='vendedor'?'seller-dashboard.html':u.rol==='administrador'?'admin-dashboard.html':'buyer-dashboard.html'):''; const nav=[['Inicio',base+'index.html','home'],['Catálogo',base+'pages/products.html','products']]; document.getElementById('app-header').innerHTML=`<header class="bg-white border-b border-slate-200 sticky top-0 z-40"><div class="cc-container flex items-center justify-between py-3 gap-3"><a href="${base}index.html" class="flex items-center gap-2"><img src="${base}assets/logo/Logo - Commercity.png" class="h-10 w-10 object-contain"><span class="font-extrabold text-xl text-blue-700">CommerCity</span></a><nav class="hidden md:flex items-center gap-4">${nav.map(n=>`<a class="${active===n[2]?'text-blue-700 font-bold':'text-slate-600'}" href="${n[1]}">${n[0]}</a>`).join('')}</nav><div class="flex items-center gap-2"><a class="btn-secondary" href="${base}pages/cart.html">Carrito (${itemCount()})</a>${u?`<a class="btn-primary" href="${base}pages/${dash}">Panel</a><button id="logoutBtn" class="btn-secondary">Salir</button>`:`<a class="btn-primary" href="${base}pages/login.html">Login</a>`}</div></div></header>`; document.getElementById('logoutBtn')?.addEventListener('click',logout); }
