
import {api,assetUrl} from './api.js'; import {shell,bindShell,$,money,toast,h,withButtonLoading} from './ui.js'; import {cart,saveCart,clearCart} from './cart-store.js'; import {requireAuth} from './auth.js';
let items=cart(), user=null, validated=null; async function render(){app.innerHTML=await shell('carrito')+`<main class="container"><h1 class="text-4xl font-extrabold mb-2">Detalles de Pago</h1><p class="muted mb-8">Carrito multi-tienda, validación de stock y pago sandbox.</p><section class="two-col"><div class="grid gap-6"><div class="card" id="cartBox"></div><form id="addrForm" class="card grid gap-3"><h2 class="text-2xl font-bold">Dirección de Envío</h2><div class="grid md:grid-cols-2 gap-3"><input class="input" name="departamento" placeholder="Departamento" required><input class="input" name="ciudad" placeholder="Ciudad" required></div><input class="input" name="direccion" placeholder="Dirección" required><div class="grid md:grid-cols-2 gap-3"><input class="input" name="telefono" placeholder="Teléfono" required><input class="input" name="codigo_postal" placeholder="Código postal"></div><label><input type="checkbox" name="es_principal" checked> Dirección principal</label></form></div><aside class="grid gap-6"><div class="card" id="summary"></div><form id="payForm" class="card grid gap-3"><h2 class="text-2xl font-bold">Pasarela de Pago <span class="pill orange">Sandbox</span></h2><input class="input" name="card_holder" value="Juan Pérez" placeholder="Titular"><input class="input" name="card_number" value="4111111111111111" placeholder="Tarjeta"><div class="grid grid-cols-3 gap-2"><input class="input" name="exp_month" value="12"><input class="input" name="exp_year" value="2030"><input class="input" name="cvv" value="123"></div><button class="btn btn-primary">Finalizar Compra 🔒</button></form></aside></section></main>`; bindShell(); try{user=await requireAuth(api,['comprador']);}catch{} draw(); validateCart().catch(()=>{}); payForm.onsubmit=pay;}
function group(){return items.reduce((a,i)=>{(a[i.tienda]??=[]).push(i);return a},{})} function draw(){cartBox.innerHTML=`<h2 class="text-2xl font-bold mb-4">Carrito</h2>${Object.entries(group()).map(([t,arr])=>`<div class="mb-5"><h3 class="font-bold mb-2">▤ ${h(t)}</h3>${arr.map(i=>`<div class="flex gap-4 items-center border-t border-orange-100 py-4"><img src="${assetUrl(i.imagen_url)}" class="w-20 h-20 rounded-2xl object-cover" alt="${h(i.nombre)}"><div class="flex-1"><b>${h(i.nombre)}</b><p>${money(i.precio)} x <button data-id="${Number(i.producto_id)||''}" class="dec">−</button> <b>${Number(i.cantidad)||0}</b> <button data-id="${Number(i.producto_id)||''}" class="inc">+</button></p></div><b>${money(i.precio*i.cantidad)}</b><button class="rm text-red-600" data-id="${Number(i.producto_id)||''}">Eliminar</button></div>`).join('')}</div>`).join('')||'<p class="muted">Carrito vacío.</p>'}`; summary.innerHTML=`<h2 class="text-2xl font-bold mb-4">Resumen del Pedido</h2><p>Subtotal: <b>${money(total())}</b></p><p>Envío: <b class="text-green-600">Gratis</b></p><div class="p-4 bg-orange-50 rounded-2xl my-4"><p>Comisión plataforma (10%): <b>${money(total()*.1)}</b></p><p>Pago vendedores (90%): <b class="text-blue-600">${money(total()*.9)}</b></p></div><p class="price">${money(total())}</p><button id="validate" class="btn btn-secondary w-full mt-3">Validar carrito</button>`; document.querySelectorAll('.inc').forEach(b=>b.onclick=()=>upd(b.dataset.id,1));document.querySelectorAll('.dec').forEach(b=>b.onclick=()=>upd(b.dataset.id,-1));document.querySelectorAll('.rm').forEach(b=>b.onclick=()=>{items=items.filter(i=>i.producto_id!=b.dataset.id);saveCart(items);draw()}); document.getElementById('validate')?.addEventListener('click',validateCart);}
function total(){return items.reduce((s,i)=>s+i.precio*i.cantidad,0)} function upd(id,d){items=items.map(i=>i.producto_id==id?{...i,cantidad:Math.max(1,i.cantidad+d)}:i);saveCart(items);draw()}
async function validateCart(){if(!items.length)return; try{validated=await api.post('/cart/validate',{items:items.map(i=>({producto_id:i.producto_id,cantidad:i.cantidad}))}); toast('Carrito validado'); return validated}catch(e){toast(e.message,'error'); throw e}}
async function pay(e){e.preventDefault(); if(!items.length)return toast('Carrito vacío','error'); const btn=payForm.querySelector('button'); await withButtonLoading(btn,async()=>{try{await validateCart(); const addr=await api.post('/addresses',Object.fromEntries(new FormData(addrForm))); const order=await api.post('/orders',{direccion_id:addr.address.id,items:items.map(i=>({producto_id:i.producto_id,cantidad:i.cantidad}))}); const fd=Object.fromEntries(new FormData(payForm)); fd.pedido_id=order.order.id; const p=await api.post('/payments/sandbox',fd); toast('Pago procesado: '+(p.payment?.estado||'OK')); clearCart(); items=[]; draw();}catch(e){toast(e.message,'error')}},'Procesando pago...')}
render();
