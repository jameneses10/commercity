
import {API_BASE_URL,API_ORIGIN} from './config.js';
import {getToken,clearSession} from './auth.js';
export function assetUrl(url){ if(!url) return 'https://images.unsplash.com/photo-1516321318423-f06f85e504b3?auto=format&fit=crop&w=900&q=70'; if(url.startsWith('http')) return url; return API_ORIGIN+url; }
async function request(path,{method='GET',body,form=false,auth=true}={}){ const headers={}; if(!form) headers['Content-Type']='application/json'; const token=getToken(); if(auth&&token) headers.Authorization=`Bearer ${token}`; const res=await fetch(API_BASE_URL+path,{method,headers,body:form?body:(body?JSON.stringify(body):undefined)}); let json={}; try{json=await res.json()}catch{} if(res.status===401){ clearSession(); if(!location.pathname.endsWith('/login.html')) location.href='login.html'; } if(!res.ok){ const msg=json.message || `Error HTTP ${res.status}`; const e=new Error(msg); e.status=res.status; e.payload=json; throw e; } return json.data ?? json; }
export const api={get:(p,o)=>request(p,{...o,method:'GET'}),post:(p,b,o)=>request(p,{...o,method:'POST',body:b}),patch:(p,b,o)=>request(p,{...o,method:'PATCH',body:b}),delete:(p,o)=>request(p,{...o,method:'DELETE'}),form:(p,fd,{method='POST',auth=true}={})=>request(p,{method,body:fd,form:true,auth})};
