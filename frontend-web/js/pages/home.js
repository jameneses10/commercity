import {renderHeader} from '../../components/header.js'; import {renderFooter} from '../../components/footer.js'; import {listCategories} from '../api/categories.api.js'; import {listProducts} from '../api/products.api.js'; import {productCard} from '../../components/product-card.js'; import {addToCart} from '../cart/cart.storage.js';
renderHeader('home'); renderFooter();
const app=document.getElementById('app'); app.innerHTML=`<section class="hero-gradient text-white"><div class="cc-container py-16 grid md:grid-cols-2 gap-8 items-center"><div><h1 class="text-4xl md:text-6xl font-extrabold">Compra y vende fácil en CommerCity</h1><p class="mt-4 text-blue-100 text-lg">Marketplace académico responsive con catálogo, carrito, pagos sandbox, envíos y reseñas.</p><a class="btn-action inline-block mt-6" href="pages/products.html">Explorar catálogo</a></div><div class="cc-card p-6 text-slate-900"><h2 class="text-2xl font-bold">Beneficios</h2><ul class="mt-4 space-y-2 text-slate-600"><li>✓ Tiendas verificadas por rol vendedor</li><li>✓ Pagos sandbox seguros</li><li>✓ Reseñas de compras entregadas</li></ul></div></div></section><section class="cc-container py-12"><h2 class="text-2xl font-bold mb-4">Categorías destacadas</h2><div id="cats" class="grid grid-cols-2 md:grid-cols-4 gap-4"></div><h2 class="text-2xl font-bold mt-10 mb-4">Productos destacados</h2><div id="products" class="grid sm:grid-cols-2 lg:grid-cols-4 gap-5"></div></section>`;
const cats=await listCategories(); document.getElementById('cats').innerHTML=(cats.data.categories||[]).slice(0,8).map(c=>`<a class="cc-card p-4 font-bold hover:text-blue-700" href="pages/products.html?category_id=${c.id}">${c.nombre}</a>`).join('');
const products=await listProducts({limit:8}); const list=products.data.products||[]; document.getElementById('products').innerHTML=list.map(p=>productCard(p,'')).join(''); document.querySelectorAll('.add-cart').forEach(btn=>btn.onclick=()=>{const p=list.find(x=>x.id==btn.dataset.id); addToCart(p); alert('Producto agregado al carrito'); renderHeader('home');});
