import {renderHeader} from '../../components/header.js'; import {renderFooter} from '../../components/footer.js'; import {register} from '../api/auth.api.js'; import {showMessage,errorText} from '../ui/messages.js';
renderHeader(); renderFooter(); document.getElementById('app').innerHTML=`<section class="cc-container py-12 max-w-lg"><div class="cc-card p-6"><h1 class="text-3xl font-bold mb-4">Crear cuenta</h1><div id="msg"></div><form id="form" class="space-y-3"><input class="input" name="nombre" placeholder="Nombre" required><input class="input" name="correo" type="email" placeholder="Correo" required><select class="input" name="rol"><option value="comprador">Comprador</option><option value="vendedor">Vendedor</option></select><input class="input" name="password" type="password" placeholder="Contraseña" required><input class="input" name="confirmPassword" type="password" placeholder="Confirmar contraseña" required><button class="btn-action w-full">Registrarme</button></form></div></section>`;
document.getElementById('form').onsubmit=async e=>{e.preventDefault(); const data=Object.fromEntries(new FormData(e.target)); if(data.password!==data.confirmPassword) return showMessage(msg,'Las contraseñas no coinciden','error'); try{await register(data); showMessage(msg,'Registro exitoso. Ahora puedes iniciar sesión.'); e.target.reset();}catch(err){showMessage(msg,errorText(err),'error')}};
