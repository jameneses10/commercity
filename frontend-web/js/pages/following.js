import {renderHeader} from '../../components/header.js'; import {renderFooter} from '../../components/footer.js'; import {following} from '../api/follow.api.js';
renderHeader(); renderFooter(); const id=new URLSearchParams(location.search).get('id'); app.innerHTML=`<section class="cc-container py-8"><h1 class="text-3xl font-bold mb-4">Siguiendo</h1><div id="list"></div></section>`; const r=await following(id); list.innerHTML=(r.data.following||[]).map(u=>`<a class="cc-card p-4 block mb-2" href="public-profile.html?id=${u.id}">${u.nombre} · ${u.rol}</a>`).join('')||'Sin seguidos';
