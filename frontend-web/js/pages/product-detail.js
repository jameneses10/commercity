import {renderHeader} from '../../components/header.js'; import {renderFooter} from '../../components/footer.js'; import {getProduct,getProductReviews} from '../api/products.api.js'; import {reportProduct} from '../api/productReports.api.js'; import {addToCart} from '../cart/cart.storage.js'; import {money,badge} from '../ui/format.js'; import {showMessage,errorText} from '../ui/messages.js';
renderHeader('products'); renderFooter(); const id=new URLSearchParams(location.search).get('id'); const app=document.getElementById('app');
try{ const r=await getProduct(id); const p=r.data.product; const rev=await getProductReviews(id); const final=p.precio_final??p.precio; const hasDiscount=Number(p.descuento_porcentaje||0)>0; app.innerHTML=`<section class="cc-container py-8 grid md:grid-cols-2 gap-8"><div class="cc-card overflow-hidden"><img class="w-full h-96 object-cover" src="${p.imagen_url||'../assets/logo/Logo - Commercity.png'}"></div><div class="cc-card p-6"><div id="msg"></div><p class="text-slate-500">${p.categoria_nombre} · ${p.tienda_nombre}</p><h1 class="text-3xl font-extrabold">${p.nombre}</h1>${hasDiscount?`<p class="text-slate-400 line-through mt-3">${money(p.precio_anterior||p.precio)}</p><p class="text-sm text-orange-600 font-bold">Descuento ${p.descuento_porcentaje}%</p>`:''}<p class="text-3xl text-blue-700 font-extrabold mt-1">${money(final)}</p><p class="mt-2">${badge(p.estado)} Stock: ${p.stock} · ⭐ ${p.calificacion_promedio}</p><p class="mt-4 text-slate-600">${p.descripcion}</p><label class="block mt-4 text-sm font-bold">Cantidad</label><input id="qty" class="input max-w-32" type="number" min="1" max="${p.stock}" value="1"><div class="mt-6 flex gap-3 flex-wrap"><button id="add" class="btn-action">Agregar al carrito</button><button id="buy" class="btn-primary">Comprar ahora</button><button id="reportBtn" class="btn-secondary">Reportar producto</button></div><form id="reportForm" class="hidden mt-4 space-y-2"><input class="input" name="motivo" placeholder="Motivo del reporte"><textarea class="input" name="descripcion" placeholder="Descripción opcional"></textarea><button class="btn-secondary">Enviar reporte</button></form></div><div class="md:col-span-2 cc-card p-6"><h2 class="text-2xl font-bold mb-3">Reseñas</h2>${(rev.data.reviews||[]).map(x=>`<div class="border-b py-3"><b>⭐ ${x.estrellas}</b><p>${x.comentario||''}</p></div>`).join('')||'Sin reseñas aún.'}</div></section>`; const addItem=()=>{const qty=Math.max(1,Math.min(Number(qtyEl.value||1),Number(p.stock||1))); addToCart({...p,precio:final},qty); alert('Agregado'); renderHeader('products')}; const qtyEl=document.getElementById('qty'); add.onclick=addItem; buy.onclick=()=>{addItem(); location.href='cart.html'}; reportBtn.onclick=()=>reportForm.classList.toggle('hidden'); reportForm.onsubmit=async e=>{e.preventDefault(); try{await reportProduct(id,Object.fromEntries(new FormData(reportForm))); showMessage(msg,'Reporte enviado al administrador.'); reportForm.reset(); reportForm.classList.add('hidden')}catch(err){showMessage(msg,errorText(err),'error')}}; }catch(e){app.innerHTML='<section class="cc-container py-8"><div class="cc-card p-8">Producto no disponible.</div></section>'}
