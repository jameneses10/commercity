import {renderHeader} from '../../components/header.js'; import {renderFooter} from '../../components/footer.js'; import {getCart,updateQty,removeItem,saveCart} from '../cart/cart.storage.js'; import {validateCart} from '../api/cart.api.js'; import {money} from '../ui/format.js'; import {showMessage,errorText} from '../ui/messages.js';
renderHeader(); renderFooter(); const app=document.getElementById('app'); function draw(){const items=getCart(); app.innerHTML=`<section class="cc-container py-8"><h1 class="text-3xl font-bold mb-4">Carrito</h1><div id="msg"></div><div class="cc-card p-4">${items.length?items.map(i=>`<div class="flex flex-col md:flex-row gap-3 items-center border-b py-3"><img class="w-20 h-20 object-cover rounded-xl bg-slate-100" src="${i.imagen_url||'../assets/logo/Logo - Commercity.png'}"><div class="flex-1"><b>${i.nombre}</b><p>${money(i.precio)} · ${i.tienda||''}</p></div><input class="input w-24 qty" data-id="${i.producto_id}" type="number" min="1" value="${i.cantidad}"><button class="btn-secondary del" data-id="${i.producto_id}">Eliminar</button></div>`).join(''):'Tu carrito está vacío.'}<div class="text-right mt-4 font-extrabold text-xl">Subtotal visual: ${money(items.reduce((a,b)=>a+b.precio*b.cantidad,0))}</div></div><div class="mt-4 flex gap-3"><button id="validate" class="btn-primary">Validar con backend</button><a id="checkout" class="btn-action" href="checkout.html">Continuar checkout</a></div></section>`; document.querySelectorAll('.qty').forEach(x=>x.onchange=()=>{updateQty(x.dataset.id,Number(x.value));draw()}); document.querySelectorAll('.del').forEach(x=>x.onclick=()=>{removeItem(x.dataset.id);draw()}); validate.onclick=async()=>{try{const r=await validateCart(getCart().map(i=>({producto_id:i.producto_id,cantidad:i.cantidad}))); localStorage.setItem('cc_cart_validated',JSON.stringify(r.data)); showMessage(msg,`Carrito validado. Total backend: ${money(r.data.total)}. Inválidos: ${r.data.invalid_items.length}`); if(r.data.invalid_items.length) localStorage.removeItem('cc_cart_validated');}catch(e){showMessage(msg,errorText(e),'error')}};} draw();
