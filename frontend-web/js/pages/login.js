import {renderHeader} from '../../components/header.js'; import {renderFooter} from '../../components/footer.js'; import {login} from '../api/auth.api.js'; import {saveSession,dashboardFor} from '../auth/session.js'; import {showMessage,errorText} from '../ui/messages.js';
renderHeader(); renderFooter(); document.getElementById('app').innerHTML=`<section class="cc-container py-12 max-w-md"><div class="cc-card p-6"><h1 class="text-3xl font-bold mb-4">Iniciar sesión</h1><div id="msg"></div><form id="form" class="space-y-3"><input class="input" name="correo" type="email" placeholder="Correo" required><input class="input" name="password" type="password" placeholder="Contraseña" required><button class="btn-primary w-full">Entrar</button></form><p class="mt-4 text-sm">¿No tienes cuenta? <a class="text-blue-700 font-bold" href="register.html">Regístrate</a></p><p class="mt-2 text-sm"><a class="text-orange-600 font-bold" href="forgot-password.html">Olvidé mi contraseña</a></p></div></section>`;
document.getElementById('form').onsubmit=async e=>{e.preventDefault(); const f=new FormData(e.target); try{const r=await login(f.get('correo'),f.get('password')); saveSession(r.data); location.href=dashboardFor(r.data.user.rol);}catch(err){showMessage(document.getElementById('msg'),errorText(err),'error')}};
