const { pool } = require('../config/database');
async function search(q){ const like=`%${q}%`; const [usuarios]=await pool.query(`SELECT u.id,u.nombre,u.correo,u.estado,r.nombre rol FROM usuarios u INNER JOIN roles r ON r.id=u.rol_id WHERE u.deleted_at IS NULL AND (u.nombre LIKE ? OR u.correo LIKE ?) ORDER BY u.created_at DESC LIMIT 10`,[like,like]); const [productos]=await pool.query(`SELECT p.id,p.nombre,p.estado,p.precio,p.stock,t.nombre tienda_nombre FROM productos p INNER JOIN tiendas t ON t.id=p.tienda_id WHERE p.nombre LIKE ? ORDER BY p.created_at DESC LIMIT 10`,[like]); const [tiendas]=await pool.query(`SELECT id,nombre,slug,estado,reputacion_promedio,nivel_reputacion FROM tiendas WHERE nombre LIKE ? ORDER BY created_at DESC LIMIT 10`,[like]); return {usuarios,productos,tiendas}; }
module.exports={search};
