const { pool } = require('../config/database');
async function dashboardStats(){ const [[roles]]=await pool.query(`SELECT SUM(r.nombre='comprador') total_compradores, SUM(r.nombre='vendedor') total_vendedores, SUM(r.nombre='administrador') total_administradores, SUM(u.estado='activo') total_usuarios_activos, SUM(u.estado IN ('inactivo','baneado','bloqueado')) total_usuarios_inactivos FROM usuarios u INNER JOIN roles r ON r.id=u.rol_id WHERE u.deleted_at IS NULL`); const [[commerce]]=await pool.query(`SELECT (SELECT COUNT(*) FROM tiendas) total_tiendas, (SELECT COUNT(*) FROM productos) total_productos, (SELECT COUNT(*) FROM productos WHERE estado='activo') total_productos_activos, (SELECT COUNT(*) FROM productos WHERE stock=0 OR estado='agotado') total_productos_agotados, (SELECT COUNT(*) FROM pedidos) total_pedidos, (SELECT COALESCE(SUM(total),0) FROM pedidos WHERE estado_pago='pagado') ventas_totales, (SELECT COALESCE(SUM(valor_comision),0) FROM comisiones) comisiones_totales, (SELECT COUNT(*) FROM reportes_productos WHERE estado='pendiente') reportes_productos_pendientes, (SELECT COUNT(*) FROM reportes_usuarios WHERE estado='pendiente') reportes_usuarios_pendientes`); return {...roles,...commerce}; }
async function updateUserStatus(id,estado){ await pool.query('UPDATE usuarios SET estado=? WHERE id=?',[estado,id]); const [[u]]=await pool.query('SELECT u.id,u.nombre,u.correo,u.estado,r.nombre rol FROM usuarios u INNER JOIN roles r ON r.id=u.rol_id WHERE u.id=?',[id]); return u||null; }
async function findUser(id){ const [[u]]=await pool.query('SELECT u.id,u.nombre,u.correo,u.estado,r.nombre rol FROM usuarios u INNER JOIN roles r ON r.id=u.rol_id WHERE u.id=?',[id]); return u||null; }
async function listUsers({limit=50,offset=0}){ const [rows]=await pool.query('SELECT u.id,u.nombre,u.correo,u.telefono,u.estado,u.created_at,r.nombre rol FROM usuarios u INNER JOIN roles r ON r.id=u.rol_id WHERE u.deleted_at IS NULL ORDER BY u.created_at DESC LIMIT ? OFFSET ?',[limit,offset]); return rows; }
module.exports={dashboardStats,updateUserStatus,findUser,listUsers};
