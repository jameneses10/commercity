const express=require('express'); const controller=require('../controllers/address.controller'); const authRequired=require('../middlewares/authRequired'); const requireRole=require('../middlewares/requireRole'); const {createAddressValidator,updateAddressValidator}=require('../validators/address.validators');
const router=express.Router(); router.get('/',authRequired,requireRole('comprador'),controller.list); router.post('/',authRequired,requireRole('comprador'),createAddressValidator,controller.create); router.patch('/:id',authRequired,requireRole('comprador'),updateAddressValidator,controller.update); router.delete('/:id',authRequired,requireRole('comprador'),controller.remove); module.exports=router;
