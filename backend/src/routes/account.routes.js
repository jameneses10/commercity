const express=require('express'); const c=require('../controllers/account.controller'); const authRequired=require('../middlewares/authRequired'); const {settingsValidator,changePasswordValidator,changeEmailValidator,deleteRequestValidator,upgradeValidator}=require('../validators/account.validators');
const router=express.Router(); router.use(authRequired); router.get('/settings',c.getSettings); router.patch('/settings',settingsValidator,c.updateSettings); router.patch('/change-password',changePasswordValidator,c.changePassword); router.patch('/change-email',changeEmailValidator,c.changeEmail); router.patch('/deactivate',c.deactivate); router.post('/delete-request',deleteRequestValidator,c.deleteRequest); router.post('/upgrade-to-seller',upgradeValidator,c.upgrade); module.exports=router;
