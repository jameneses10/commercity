import {renderHeader} from '../../components/header.js'; import {renderFooter} from '../../components/footer.js'; import {requireAuth} from '../auth/guards.js'; import {getCart,clearCart} from '../cart/cart.storage.js'; import {apiRequest} from '../api/client.js'; import {createOrder} from '../api/orders.api.js'; import {sandboxPayment} from '../api/payments.api.js'; import {validateCart} from '../api/cart.api.js'; import {money} from '../ui/format.js'; import {showMessage,errorText} from '../ui/messages.js';
const user=requireAuth('comprador'); renderHeader(); renderFooter(); if(user){ const app=document.getElementById('app'); app.innerHTML=`<section class="cc-container py-8"><h1 class="text-3xl font-bold mb-4">Checkout seguro</h1><div id="msg"></div><div class="grid lg:grid-cols-2 gap-6"><div class="cc-card p-5"><h2 class="font-bold text-xl mb-3">Dirección</h2><select id="addresses" class="input mb-3"></select><div class="grid gap-2"><input id="dep" class="input" placeholder="Departamento"><input id="city" class="input" placeholder="Ciudad"><input id="addr" class="input" placeholder="Dirección"><input id="phone" class="input" placeholder="Teléfono"><button id="newAddr" class="btn-secondary">Crear dirección</button></div></div><div class="cc-card p-5"><h2 class="font-bold text-xl mb-3">Pago sandbox</h2><p class="text-sm text-slate-500 mb-3">4111111111111111 aprueba · 4000000000000002 rechaza. No uses tarjetas reales.</p><input id="card" class="input mb-2" placeholder="Número de tarjeta de prueba"><input id="holder" class="input mb-2" placeholder="Titular"><div class="grid grid-cols-3 gap-2"><input id="month" class="input" value="12"><input id="year" class="input" value="2030"><input id="cvv" class="input" placeholder="CVV"></div><button id="pay" class="btn-action w-full mt-4">Crear pedido y pagar</button></div></div></section>`; async function loadAddr(){const r=await apiRequest('/addresses',{auth:true}); addresses.innerHTML=r.data.addresses.map(a=>`<option value="${a.id}">${a.ciudad} - ${a.direccion}</option>`).join('')} await loadAddr().catch(()=>{}); newAddr.onclick=async()=>{try{await apiRequest('/addresses',{method:'POST',auth:true,body:{departamento:dep.value,ciudad:city.value,direccion:addr.value,telefono:phone.value}}); await loadAddr(); showMessage(msg,'Dirección creada')}catch(e){showMessage(msg,errorText(e),'error')}}; pay.onclick=async()=>{try{const items=getCart().map(i=>({producto_id:i.producto_id,cantidad:i.cantidad})); const valid=await validateCart(items); if(valid.data.invalid_items.length) throw new Error('Carrito inválido, revisa stock o estado.'); const order=await createOrder({direccion_id:Number(addresses.value),items}); const pay=await sandboxPayment({pedido_id:order.data.order.id,card_number:card.value,card_holder:holder.value,exp_month:Number(month.value),exp_year:Number(year.value),cvv:cvv.value}); if(pay.data.payment.estado==='aprobado'){clearCart(); showMessage(msg,`Pago aprobado. Total: ${money(valid.data.total)}. Pedido #${order.data.order.id}`); renderHeader();}else showMessage(msg,'Pago rechazado por sandbox','error'); card.value=cvv.value='';}catch(e){showMessage(msg,errorText(e),'error')}}; }
