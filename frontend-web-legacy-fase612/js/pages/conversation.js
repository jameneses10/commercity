import {renderHeader} from '../../components/header.js'; import {renderFooter} from '../../components/footer.js'; import {requireAuth} from '../auth/guards.js'; import {getMessages,sendMessage,markConversationRead} from '../api/chat.api.js'; import {showMessage,errorText} from '../ui/messages.js';
const user=requireAuth(); renderHeader('chat'); renderFooter(); const id=new URLSearchParams(location.search).get('id'); if(user){app.innerHTML=`<section class="cc-container py-8"><a class="text-blue-700" href="chat.html">← Volver al chat</a><h1 class="text-3xl font-bold mb-4">Conversación #${id}</h1><div id="msg"></div><div id="messages" class="cc-card p-5 space-y-2 mb-4"></div><form id="form" class="flex gap-2"><input class="input" name="mensaje" placeholder="Escribe un mensaje"><button class="btn-primary">Enviar</button></form></section>`; async function load(){const r=await getMessages(id); messages.innerHTML=(r.data.messages||[]).map(m=>`<div class="${m.emisor_id===user.id?'text-right':''}"><span class="inline-block rounded-xl px-3 py-2 ${m.emisor_id===user.id?'bg-blue-600 text-white':'bg-slate-100'}">${m.mensaje}</span><p class="text-xs text-slate-400">${m.emisor_nombre}</p></div>`).join('')||'Sin mensajes'; await markConversationRead(id);} form.onsubmit=async e=>{e.preventDefault(); const d=Object.fromEntries(new FormData(form)); try{await sendMessage(id,d); form.reset(); await load()}catch(er){showMessage(msg,errorText(er),'error')}}; await load();}
