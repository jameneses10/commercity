import {renderHeader} from '../../components/header.js'; import {renderFooter} from '../../components/footer.js'; import {requireAuth} from '../auth/guards.js'; import {getSettings,updateSettings,changePassword,deactivateAccount,upgradeToSeller} from '../api/account.api.js'; import {showMessage,errorText} from '../ui/messages.js'; import {clearSession,saveSession} from '../auth/session.js';
const user=requireAuth(); renderHeader(); renderFooter(); if(user){ app.innerHTML=`<section class="cc-container py-8 max-w-4xl"><h1 class="text-3xl font-bold mb-4">Ajustes de cuenta</h1><div id="msg"></div><div class="grid lg:grid-cols-2 gap-6"><div class="cc-card p-5"><h2 class="font-bold text-xl mb-3">Datos básicos</h2><form id="settingsForm" class="space-y-3"><input class="input" name="nombre" placeholder="Nombre"><input class="input" name="telefono" placeholder="Teléfono"><button class="btn-primary">Guardar</button></form></div><div class="cc-card p-5"><h2 class="font-bold text-xl mb-3">Contraseña</h2><form id="passForm" class="space-y-3"><input class="input" name="currentPassword" type="password" placeholder="Contraseña actual"><input class="input" name="newPassword" type="password" placeholder="Nueva contraseña"><input class="input" name="confirmPassword" type="password" placeholder="Confirmar"><button class="btn-secondary">Cambiar contraseña</button></form></div><div class="cc-card p-5"><h2 class="font-bold text-xl mb-3">Vendedor</h2><p class="text-sm text-slate-500 mb-3">Puedes cambiar de comprador a vendedor aceptando condiciones. No se crea tienda automáticamente.</p><label class="text-sm flex gap-2"><input id="sellerTerms" type="checkbox"> Acepto condiciones de vendedor</label><button id="upgradeBtn" class="btn-action mt-3">Pasarme a vendedor</button></div><div class="cc-card p-5 border-red-200"><h2 class="font-bold text-xl mb-3 text-red-600">Desactivar cuenta</h2><p class="text-sm text-slate-500 mb-3">Es una inactivación lógica. No borra pedidos históricos.</p><button id="deactivateBtn" class="btn-secondary">Desactivar cuenta</button></div></div></section>`; const r=await getSettings(); settingsForm.nombre.value=r.data.user.nombre||''; settingsForm.telefono.value=r.data.user.telefono||''; settingsForm.onsubmit=async e=>{e.preventDefault(); try{const up=await updateSettings(Object.fromEntries(new FormData(settingsForm))); saveSession({token:localStorage.getItem('cc_token'), user:up.data.user}); showMessage(msg,'Ajustes actualizados.')}catch(err){showMessage(msg,errorText(err),'error')}}; passForm.onsubmit=async e=>{e.preventDefault(); try{await changePassword(Object.fromEntries(new FormData(passForm))); showMessage(msg,'Contraseña cambiada.')}catch(err){showMessage(msg,errorText(err),'error')}}; upgradeBtn.onclick=async()=>{try{await upgradeToSeller({acceptSellerTerms:sellerTerms.checked}); showMessage(msg,'Ahora eres vendedor. Vuelve a iniciar sesión para actualizar tu panel.')}catch(err){showMessage(msg,errorText(err),'error')}}; deactivateBtn.onclick=async()=>{if(confirm('¿Desactivar tu cuenta?')){try{await deactivateAccount(); clearSession(); location.href='login.html'}catch(err){showMessage(msg,errorText(err),'error')}}}; }
